// URL central del backend para toda la app.
// Usamos /api para que funcione en localhost y también en Dev Tunnels.
// Vite recibe /api y lo reenvía al backend que corre en localhost:3001.
export const API_URL = '/api';
